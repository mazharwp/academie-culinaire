jQuery(document).ready(function($) {    // Function to fetch filter options via AJAX    function fetchFilterOptions(filterName, callback) {        $.ajax({            url: ajax_object.ajax_url,            type: 'POST',            data: {                action: 'fetch_filter_options',                filter: filterName            },            success: function(response) {                callback(response);            },            error: function(errorThrown) {                console.log('Error fetching ' + filterName + ' options: ' + errorThrown);            }        });    }    // Populate Category filter    fetchFilterOptions('category', function(options) {        $('.dropdown-category').siblings('.dropdown__select').empty();        options.forEach(function(option) {            $('.dropdown-category').siblings('.dropdown__select').append('<li class="dropdown__select-option" role="option">' + option.name + '</li>');        });    });    // Populate Price filter    fetchFilterOptions('price', function(options) {        $('.price__filter-selected').siblings('.dropdown__select').empty();        options.forEach(function(option) {            $('.price__filter-selected').siblings('.dropdown__select').append('<li class="dropdown__select-option" role="option">' + option.name + '</li>');        });    });    // Populate Size filter    fetchFilterOptions('size', function(options) {        $('.size__filter-selected').siblings('.dropdown__select').empty();        options.forEach(function(option) {            $('.size__filter-selected').siblings('.dropdown__select').append('<li class="dropdown__select-option" role="option">' + option.name + '</li>');        });    });    // Populate Color filter    fetchFilterOptions('color', function(options) {        $('.color__filter-selected').siblings('.dropdown__select').empty();        options.forEach(function(option) {            $('.color__filter-selected').siblings('.dropdown__select').append('<li class="dropdown__select-option" role="option">' + option.name + '</li>');        });    });    // Add click event to Clear All link (example)    $('a[href="javascript:void(0)"]').click(function() {        // Implement clear functionality as needed        // Example: Reset selected filters        $('.dropdown__select li').removeClass('selected');    });});